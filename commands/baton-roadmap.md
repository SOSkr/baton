---
description: Plan and read the roadmap on the board — epics with target dates, live progress
---
Activate the baton-roadmap skill: $ARGUMENTS

Follow the baton-roadmap workflow: read with `baton groups` and
`baton list --group "<epic>"`, create epics deliberately (name + target date +
description from templates/epic.md), assign items with `baton group <id> --to "<epic>"`.
Never write a roadmap document — the board is the roadmap. Report only the cross-checks
the board cannot do itself.
