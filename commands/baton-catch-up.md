---
description: Recover what already happened on a work-item, or in another project, before asking anyone
---
Activate the baton-catch-up skill for: $ARGUMENTS

Board first — `baton show <id> --comments`, or `baton --project <name> list` when
there is no item id. Then enrich with the session-memory store if one is available.
Report which layers you actually used; a thin answer that looks complete is worse
than a declared gap.
