<!-- A bug's Verification is not optional even for a human: the repro IS the check. -->

## Current behavior
What happens. Exact error text, quoted verbatim — never paraphrased.

## Expected behavior
What should happen instead.

## Reproduction
1. <step>
2. <step>
→ observed: <what you get>

Environment: <version / branch / backend / OS, whatever is load-bearing>

## Evidence
Log line, screenshot, failing test, `file.py:42`.

## Verification
The reproduction above no longer reproduces, plus:
`<the regression test or command>` — expected: <result>.

## Out of scope
`<path/or/area>` — the neighbouring smell someone will be tempted to fix here.
Fix the root cause of THIS bug, not everything it touches.
