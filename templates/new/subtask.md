<!-- A subtask is a work-item with a PARENT — Plane's native sub-issue, not a label.
     No user story: it is inherited from the parent. If this needs its own user
     story, it is not a subtask, it is a task. -->

## Parent
<#id> — <one line: which part of it this is>

## What
The concrete change. One sitting's worth of work.

## Acceptance criteria
- [ ] <observable condition>

## Verification
`<command>` — expected: <result>.

## Out of scope
<what belongs to a sibling subtask, not this one — named as behaviour or module,
not a file path.>
